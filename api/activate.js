require('dotenv').config();const m=require('mongoose');const U=require('./src/models/User');m.connect(process.env.MONGODB_URI).then(async()=>{const u=await U.findOne({email:'estebanz@hudsonriversolar.com'});if(\!u){console.log('Not found');process.exit(1);}u.isActive=true;u.emailVerified=true;u.emailVerificationToken=undefined;u.emailVerificationExpires=undefined;await u.save();console.log('Activated:',u.email);process.exit(0);});
